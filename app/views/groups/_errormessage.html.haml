- if @group.errors.any?
  .chat-group-form__errors
    #error_explanation
      %h2
        = @group.errors.count
        件のエラーがあります。
      %ul
        - @group.errors.full_messages.each do |msg|
          %li= msg
